{
    "ConfigSection": "YetAnotherClanMech",
    "DisplayName": "Yet Another Clan Mech",
    "UIElements": [
		{
			"Var": "disableFixedOmniInternals",
			"Type": "Toggle",
			"Default": false,
			"Name": "No fixed internals on Omnimechs (requires restart, does not apply to inventory)",
			"Tooltip": "Normally Omnimechs have fixed internals. This will replace the fixed internals with standard components which can be replaced in the mechlab.\nDoes only apply to default loadouts after a restart. Mechs in compaign or career inventory are not affected."
		}
	]
}
